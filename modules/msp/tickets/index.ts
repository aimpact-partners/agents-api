import type { IUserBase as User } from '@aimpact/agents-api/data/interfaces';

export /*bundle*/ class Tickets {
	static async save(params, user: User) {}
	static async get(id, user: User) {}
	static async list(user: User) {}
}
